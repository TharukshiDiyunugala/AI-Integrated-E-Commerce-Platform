import React from 'react';
import { Button } from '@mui/material';

function Homepage() {
  return (
    <div>
      <h1>Homepage</h1>
      <Button variant="contained" color="primary">Shop Now</Button>
      {/* Add categories, banners, and featured products here */}
    </div>
  );
}

export default Homepage;
